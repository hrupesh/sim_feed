import React, { Component } from "react";
import { Text, View } from "react-native";

export class Strips extends Component {
  render() {
    return (
      <View>
        <Text> Strip Component works! </Text>
      </View>
    );
  }
}

export default Strips;
